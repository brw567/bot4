# Bot4 Trading Platform - Security-First .gitignore
# CRITICAL: Prevents secrets and artifacts from reaching GitHub

# ============================================
# SECRETS & CONFIGURATION (NEVER COMMIT)
# ============================================
.env
.env.*
!.env.example
*.env
secrets/
**/secrets/
config/*.json
!config/*.example.json
config/*.yaml
!config/*.example.yaml
config/*.toml
!config/*.example.toml

# API Keys and Credentials
*_api_key*
*_secret*
*.key
*.pem
*.p12
*.pfx
*.cert
*.crt
credentials/
tokens/

# Database URLs and passwords
database.url
db_config.json
connection_strings.txt

# ============================================
# RUST BUILD ARTIFACTS (LARGE FILES)
# ============================================
# CRITICAL: Never commit compiled artifacts!
target/
**/target/
**/target/**
rust_core/target/
rust_core/target/**
rust_core/target/debug/
rust_core/target/release/
rust_core/target/x86_64-unknown-linux-gnu/
rust_core/target/x86_64-*/
rust_core/target/*/
Cargo.lock
**/*.rs.bk
*.pdb

# Rust build files (60+ MB files identified)
*.rlib
*.rmeta
*.d
*.so
*.dylib
*.dll
*.exe

# Incremental compilation
**/incremental/
**/deps/
**/*.bin

# Benchmark and test artifacts
**/criterion/
**/tarpaulin/
**/*.profraw
**/*.profdata

# Build cache
**/.fingerprint/
**/build/

# CRITICAL: Additional safety patterns for Rust artifacts
# These ensure nothing from compilation gets committed
rust_core/target/debug/deps/*
rust_core/target/release/deps/*
rust_core/target/debug/build/*
rust_core/target/release/build/*
rust_core/target/debug/incremental/*
rust_core/target/release/incremental/*
rust_core/target/.rustc_info.json
rust_core/target/CACHEDIR.TAG

# Binary executables (should never be committed)
rust_core/target/debug/bot4*
rust_core/target/release/bot4*
rust_core/target/debug/trading*
rust_core/target/release/trading*

# ============================================
# LOGS & RUNTIME DATA
# ============================================
logs/
*.log
*.log.*
log-*.txt
debug.log
error.log
access.log
trading.log

# Monitoring data
prometheus_data/
grafana_data/
loki_data/
jaeger_data/

# ============================================
# DOCKER & CONTAINERS
# ============================================
postgres_data/
timescale_data/
redis_data/
docker/volumes/
*.dump
*.sql.gz

# ============================================
# ARCHIVED/OLD CODE
# ============================================
rust_core_old_epic7/
rust_core_old*/
*_archived_*/
deprecated/
legacy/

# ============================================
# TEMPORARY FILES & SESSION DATA
# ============================================
SESSION_*.md
CLAUDE_SESSION_*.md
*_TEMP.md
*_BACKUP.md
.tmp/
tmp/
temp/

# Test artifacts
test_simd
test_*.rs
*.test
*.bench

# Temporary scripts
fix_*.sh
patch_*.sh
temp_*.sh

# Editor and IDE files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
